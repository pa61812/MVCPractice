@{
    ViewBag.Title = "Home Page";
}
@using Architecture.Common
@model IEnumerable<Architecture.Common.Member>
<br />

<table class="table">
    <tr>

        <td>帳號</td>
        <td>密碼</td>
        <td>人名</td>
        <td>手機</td>
        <td>電話</td>
        <td>性別</td>
        <td>生日</td>
        <td> </td>
    </tr>
    <tbody>
        @foreach (var item in Model)
        {
            @Html.Partial("_PartialAllUser", item);
        }
    </tbody>
</table>


@section scripts{
    <script type="text/javascript">


        $(document).ready(function () {

            $("input[name$='update']").click(function () {
                alert("test");
            })
            //Load();
            //function Load() {
            //    $.ajax({
            //        url: "/Home/AllUser",
            //        type: "Get",
            //        dataType: "JSON",
            //        success: function (data) {
            //            var str = "";

            //            for (var i in data) {

            //                str = "<tr><td>" + data[i]["Account"] + "</td><td>" + data[i]["Password"] +
            //                    "</td><td>" + data[i]["Name"] + "</td><td>" + data[i]["Phone"] +
            //                    "</td><td>" + data[i]["Tel"] + "</td><td>" + data[i]["Gender"] +
            //                    "</td><td>" + data[i]["Birthday"] +
            //                    "</td><td>" + "<input type=" + "button" + " name=" + "update" + " class=" + "bg-success" + " value=" + "修改 " + "/>"
            //                    + "<input type=" + "button" + " name=" + "delete" + " class=" + "bg-danger" + " value=" + "刪除 " + "/>" + "</td></tr>";
            //                $("#bg").html(str);
            //            }

            //        }
            //    });
            //}

        })
    </script>
}
